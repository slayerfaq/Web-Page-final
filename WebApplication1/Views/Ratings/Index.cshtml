@model IEnumerable<WebApplication1.Models.Ratings>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Data_lessons)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Disciplina.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Data_lessons)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Disciplina.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_student }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_student }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_student })
        </td>
    </tr>
}

</table>
